@page "/social/instagram"
@{
    ViewData["Title"] = "Instagram Marketing";
    ViewData["BodyClass"] = "page-social-instagram";
}

<style>
    .soc-shell { max-width: 1600px; margin: 0 auto; display: grid; gap: 18px; padding-bottom: 28px; }
    .soc-hero, .soc-panel {
        border-radius: 30px;
        border: 1px solid rgba(255,255,255,0.1);
        background:
            radial-gradient(720px 260px at -8% -18%, rgba(244,114,182,0.16), transparent 54%),
            radial-gradient(560px 220px at 108% 0%, rgba(251,191,36,0.16), transparent 58%),
            linear-gradient(145deg, rgba(10,10,10,0.985), rgba(4,4,4,0.995));
        box-shadow: 0 30px 80px rgba(0,0,0,0.48);
    }
    .soc-hero { padding: 28px; }
    .soc-kicker { display:inline-flex; align-items:center; gap:10px; padding:8px 14px; border-radius:999px; border:1px solid rgba(255,255,255,.1); background:rgba(255,255,255,.04); color:#f472b6; font-size:.72rem; font-weight:900; letter-spacing:.18em; text-transform:uppercase; }
    .soc-title { margin:14px 0 10px; color:#f5f7fa; font-family:"Cabinet Grotesk",sans-serif; font-size:clamp(2rem,3.6vw,4rem); line-height:.92; letter-spacing:-.05em; }
    .soc-copy { margin:0; color:#95a1b3; line-height:1.7; max-width:900px; }
    .soc-grid { display:grid; grid-template-columns:1.05fr .95fr; gap:18px; }
    .soc-panel { padding:22px; }
    .soc-panel h3 { margin:0 0 8px; color:#f5f7fa; font-family:"Cabinet Grotesk",sans-serif; font-size:1.5rem; }
    .soc-panel p { margin:0; color:#95a1b3; }
    .soc-list { display:grid; gap:12px; margin-top:16px; }
    .soc-item { border-radius:18px; border:1px solid rgba(255,255,255,.08); background:rgba(255,255,255,.025); padding:14px; color:#dfe6ef; }
    .soc-item strong { display:block; margin-bottom:6px; color:#fff; }
    @@media (max-width: 1100px) { .soc-grid { grid-template-columns:1fr; } }
</style>

<div class="soc-shell">
    <section class="soc-hero">
        <span class="soc-kicker">Channel Expansion</span>
        <h1 class="soc-title">Instagram Marketing is staged on the same premium operating system as Facebook.</h1>
        <p class="soc-copy">The channel shell is now in place. The next activation wave will reuse the same lead operations, attribution reporting, action drawer patterns, and workflow automation contracts already established in Facebook Marketing.</p>
    </section>

    <section class="soc-grid">
        <article class="soc-panel">
            <h3>What this channel will ship with</h3>
            <div class="soc-list">
                <div class="soc-item"><strong>Ad and form inventory</strong>Synced campaign, ad set, ad, and form visibility with operational filters.</div>
                <div class="soc-item"><strong>Leads workspace</strong>Lead queue, timeline, operator actions, and automation triggers.</div>
                <div class="soc-item"><strong>Attribution reporting</strong>Qualified rate, booking rate, response speed, and owner performance.</div>
            </div>
        </article>
        <article class="soc-panel">
            <h3>Current status</h3>
            <div class="soc-list">
                <div class="soc-item"><strong>Design system</strong>Aligned with the black-theme Facebook Marketing experience.</div>
                <div class="soc-item"><strong>Runtime model</strong>Will reuse the normalized lead context and social ops infrastructure.</div>
                <div class="soc-item"><strong>Activation path</strong>Integration connection, webhook mapping, and asset sync are the next enablement steps.</div>
            </div>
        </article>
    </section>
</div>
